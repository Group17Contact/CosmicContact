deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/
    - export BACKUPPATH=$HOME/backup/
    - mkdir $BACKUPPATH/$(date '+%F-%H-%M-%S')
    - /bin/mv $DEPLOYPATH $BACKUPPATH/$(date '+%F-%H-%M-%S')
    - /bin/mkdir $DEPLOYPATH
    - /bin/cp -R ./ $DEPLOYPATH